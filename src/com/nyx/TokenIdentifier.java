package com.nyx.TokenIdentifier;

enum TokenIdentifier {
	// LITERALS
	/*
	Literally gonna code this up in about 3-12 hours so
	 */
	INT, STRING, IDENTIFIER

	// KEYWORDS
	VAR, CLASS, FUNCTION, IF, ELSE, TRUE, FALSE,
}